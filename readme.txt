To run the application you need Node.js and NPM.

* downloaded the recommended version of node- https://nodejs.org/en/

* clone the repository. Open the project on any IDE and run command- npm install
This will install all the dependency files.

* After successful npm install, run command- npm run dev
this will run the application on port  http://localhost:5173/ which can be opened on any browser.